# Clubs Environment Variables

# Path to the Caddyfile to manage
# Default: ./config/Caddyfile
CADDYFILE_PATH=./config/Caddyfile

# Caddy Admin API URL (for Live Mode)
# Default: http://localhost:2019
CADDY_API_URL=http://localhost:2019

# Path to Caddy's certificates directory (for viewing SSL certificates)
# Must be mounted as a volume in docker-compose (read-only is recommended)
# Default: /data/caddy/certificates
CADDY_CERTIFICATES_PATH=/data/caddy/certificates

# Node environment
# Options: development, production, test
NODE_ENV=development
