# Clubs Environment Variables

# Path to the Caddyfile to manage
# Default: ./config/Caddyfile
CADDYFILE_PATH=./config/Caddyfile

# Caddy Admin API URL (for Live Mode)
# Default: http://localhost:2019
CADDY_API_URL=http://localhost:2019

# Node environment
# Options: development, production, test
NODE_ENV=development

# Authentication & Database
# JWT secret for signing access tokens (OPTIONAL - auto-generated if not provided)
# If not set, a secure random secret will be generated and stored in the database
# Only set this if you need to share the same secret across multiple instances
# JWT_SECRET=your-custom-secret-here

# Database file path (SQLite) - OPTIONAL
# Default: ./data/clubs.db
# DATABASE_PATH=./data/clubs.db
